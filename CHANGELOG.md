# Changelog

이 프로젝트의 모든 주요 변경사항을 이 파일에 기록합니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)을 따르며,
[유의적 버전(SemVer)](https://semver.org/lang/ko/)을 사용합니다.

- **MAJOR**: 기존 사용자 데이터/사용 흐름과 호환되지 않는 변경
- **MINOR**: 하위 호환되는 기능 추가
- **PATCH**: 하위 호환되는 버그 수정

## [Unreleased]

## [1.18.1] - 2026-09-22

### Fixed
- **우측 플로팅 버튼 팝업 상호 배타 처리** — 📊 월간 연차 현황 / 📈 월별 제품 통계 팝업이 동시에 열려 겹치던 문제 — 하나를 열면 다른 하나는 자동으로 닫히도록 수정

## [1.18.0] - 2026-09-19

### Added
- **근태관리 — 확장 설치 안내** — 근태관리 패널 헤더에 [🧩 확장 설치] 버튼 추가. Chrome 확장 설치 방법을 팝업으로 안내(이 PC/다른 PC 모드), 다른 PC용으로 확장 zip(`calendar-extension.zip`) 다운로드 지원

## [1.17.0] - 2026-08-25

### Added
- **복사 시각 표시** — [일정표 복사] / [사내 게시판용] / [이미지로 복사]로 복사한 결과물(붙여넣은 표/이미지) 우측 상단에 복사 시각을 "YYYY-MM-DD hh:mm 기준" 형식으로 표시

## [1.16.0] - 2026-08-12

### Changed
- **근태관리 — 레이아웃/디자인 다듬기** — 섹션을 카드 형태로 구분(배경·테두리·좌측 강조선), 안내 문구를 색상 점 칩(chip) 형태로 변경, 연차 현황 표를 좁은 화면(≤640px)에서 카드형으로 전환, 그래프 기간 컨트롤에 배경 적용, "직급" 열 이름을 "직책"으로 변경, 표 헤더 타이틀을 가운데 정렬로 통일, 출퇴근 현황 표 헤더에 sticky 적용(패널 전체 스크롤 하나로 이어짐, 별도 스크롤 영역 없음), 월 선택 입력창을 자체 제작 드롭다운(년도 이동 + 12개월 그리드)으로 교체해 네이티브 달력의 "지우기" 링크 제거, 출퇴근 현황 표를 좁은 화면(≤640px)에서 날짜별 카드 + 팀원별 한 줄 목록으로 전환해 모바일 가독성 개선, 월간 근무시간 그래프에 인정 근무 기준 순위 배지 추가

## [1.15.19] - 2026-08-11

### Changed
- **근태관리 — 재직기간/경력 표기 통일** — 연차 현황의 "총 재직기간"·"총 경력"을 그룹웨어 원문 표기(선행 0이 붙거나 공백 없는 등 제각각)에서 "3년 11개월" 형식(선행 0 제거, 공백 추가)으로 통일

## [1.15.18] - 2026-08-11

### Changed
- **근태관리 — "9시 보정" 라벨을 "보정"으로 축약** — 출퇴근 현황(일자별) 각 칸의 라벨을 줄여 한 줄 길이를 줄임

## [1.15.17] - 2026-08-11

### Changed
- **근태관리 — 일자별 표 시간 3줄 좌측 정렬 및 라벨 명시** — 출퇴근 현황(일자별) 각 칸의 인정/9시 보정/연장 시간 3줄을 좌측 정렬로 변경, "인정: XX시간 X분" / "9시 보정: XX시간 XX분" / "연장: XX시간 XX분" 형식으로 라벨 명시

## [1.15.16] - 2026-08-11

### Changed
- **근태관리 — 안내 문구 좌측 정렬** — "월간 근무시간 합계" 아래 안내 문구 3줄을 우측 정렬에서 좌측 정렬로 변경
- **근태관리 — 연차 사용/잔여 색상 구분** — 연차 현황 표에서 "사용"은 파란색, "잔여"는 빨간색으로 구분 표시

## [1.15.15] - 2026-08-11

### Changed
- **근태관리 — 월간 합계를 그룹웨어 원본 그대로 집계** — "인정 근무"·"연장 근무" 월간 합계에 걸려 있던 자체 필터(출퇴근 기록 없이 주말에만 인정근무가 찍히는 이상치 제외, 출퇴근 기록 있는 날만 연장근무 집계)를 제거. 9시 기준 합계를 제외한 모든 값이 그룹웨어 원본을 그대로 합산하도록 통일

## [1.15.14] - 2026-08-11

### Added
- **근태관리 — 연차 현황에 입사일/총 재직기간/총 경력 추가** — 연차 현황 표에 이름 우측으로 입사일 · 총 재직기간 · 총 경력 열 추가(그룹웨어 `employeeInfoList` 기준). 새 Firebase 경로 `/employee_info`에 admin 전용 읽기/쓰기 규칙 추가, `sync-attendance.js`가 출퇴근 동기화 시 함께 갱신(자주 안 바뀌는 정보라 매번 최신화)

## [1.15.13] - 2026-08-11

### Added
- **근태관리 — 연장 근무 시간 추가** — 그룹웨어 연장근무(overtime) 값을 월간 근무시간 합계 표에 새 열로, 일자별 표 각 칸에도 함께 표시. 실제 출퇴근 기록이 있는 날만 집계(휴가일·주말 이상치 제외). 그래프는 인정 근무/9시 기준과 크기 단위가 달라 혼동을 피하고자 표에만 추가

## [1.15.12] - 2026-08-11

### Changed
- **근태관리 — 9시 기준 합계에도 인정 근무 반영** — 출퇴근 기록이 없는 날(연차 등 인정 근무만 있는 날)을 9시 기준 합계에서 0으로 빠뜨리던 것을 수정. 그런 날은 인정 근무 시간을 9시 기준 합계에도 그대로 더함. 실제 출퇴근이 있는 날의 계산 방식(9시 이전 조기출근 제외, 점심 1시간 제외)은 기존과 동일

## [1.15.11] - 2026-08-11

### Changed
- **근태관리 — "기록상 합계" → "인정 근무" 표기 변경** — 표 헤더/그래프 범례/툴팁의 "기록상 합계" 표기를 "인정 근무"로 변경(계산 방식 자체는 그룹웨어 인정근무 기준으로 이전과 동일)
- **근태관리 — 안내 문구 줄바꿈** — 한 줄로 붙여 쓰던 안내 문구를 "인정 근무" 설명과 "9시 기준" 설명 2줄로 분리 표시

## [1.15.10] - 2026-08-11

### Changed
- **근태관리 — 기록상 합계를 그룹웨어 인정근무 기준으로 교체** — "기록상 합계"를 실제 출퇴근 시각으로 직접 계산하던 방식에서 그룹웨어의 인정근무(recognizedWork) 값을 그대로 사용하도록 변경. 연차 등 휴가로 출퇴근 기록이 없는 날도 정확히 반영됨(예: 종일 연차는 8시간, 반차+근무는 근무시간+휴가시간 합산). "9시 기준 합계"는 기존처럼 실제 출퇴근 시각 기준 유지

### Fixed
- **동기화 스크립트 — 월 경계 데이터 누수** — `sync-attendance.js`가 그룹웨어 API로부터 요청한 달 범위 밖 날짜를 함께 받아와 다른 달 데이터에 섞여 들어가던 문제 수정
- **근태관리 — 주말 이상치 제외** — 실제 출퇴근 기록 없이 주말에만 인정근무가 찍히는 이상치 데이터가 월간 합계에 섞이지 않도록 제외

## [1.15.9] - 2026-08-11

### Fixed
- **근태관리 — 출퇴근 현황에 토/일 포함 전체 날짜 표시** — 일자별 표가 그룹웨어에 기록이 있는 날짜만 보여줘서 토/일이 빠지고 들쭉날쭉했던 문제. 이제 그 달 1일부터 말일까지 전체 날짜를 순서대로 표시하고, 기록 없는 날은 "-"로 표시

## [1.15.8] - 2026-08-11

### Added
- **근태관리 — 그래프 기간을 연도/월 구간으로 확장** — 월간 근무시간 그래프에 "그래프 기간" 시작~종료 달 선택과 "올해 전체" 버튼 추가. 선택한 기간(여러 달) 전체를 합산해 팀원별 기록상/9시 기준 합계를 비교. 표와 일자별 목록은 기존처럼 단일 달을 그대로 유지하고, 그래프만 별도 기간으로 조회 가능

## [1.15.7] - 2026-08-11

### Added
- **근태관리 — 월간 근무시간 그래프** — 월간 근무시간 합계 표 아래에 팀원별 묶은막대 그래프 추가. 기록상 합계(파랑)와 9시 기준 합계(주황)를 나란히 비교, 막대에 마우스를 올리면 정확한 시간(툴팁) 확인 가능

## [1.15.6] - 2026-08-11

### Added
- **근태관리 — 최초 출근/최종 퇴근 강조** — 일자별 표에서 그날 가장 일찍 출근한 시각과 가장 늦게 퇴근한 시각을 옅은 파란색으로 강조 표시

### Changed
- **근태관리 — 섹션 순서 변경** — 연차 현황 → 출퇴근 현황(일자별) → 월간 근무시간 합계 순으로 재배치(월 선택기는 출퇴근 현황 섹션으로 이동)

## [1.15.5] - 2026-08-11

### Fixed
- **근태관리 — 일자별 근무시간 줄바꿈 정리** — 일자별 표 칸에서 "기록상 / 9시 기준" 근무시간이 한 줄에 붙어 있어 좁은 칸에서 어색하게 줄바꿈되던 문제. 각각 별도 줄로 표시

## [1.15.4] - 2026-08-11

### Changed
- **근태관리 — 레이아웃 정리** — "출퇴근 현황"(일자별 표)을 맨 아래로 이동. 월간 근무시간 합계가 먼저 보이고, 상세 일자별 표는 그 아래에 위치

### Added
- **근태관리 — 일자별 근무시간 표시** — 일자별 표의 각 칸에 그날의 기록상/9시 기준 근무시간을 함께 표시(기존엔 월 합계에서만 확인 가능했음)

## [1.15.3] - 2026-08-11

### Added
- **근태관리 — 월간 총 근무시간 합계** — 출퇴근 현황 위에 팀원별 월간 총 근무시간 표 추가(점심 1시간 제외). "기록상 합계"(실제 출퇴근 그대로)와 "9시 기준 합계"(오전 9시 이전 조기출근분 제외) 2가지를 나란히 비교

## [1.15.2] - 2026-08-11

### Fixed
- **근태관리 — 월 선택 반영** — 팝업 안 월(month) 선택기를 바꿔도 이전 달 데이터만 계속 보이던 문제. 선택한 달로 바로 Firebase 리스너를 갈아끼우도록 수정
- **근태관리 — 지각 기준 정확화** — 빨간색 지각 표시가 "09시 이후 출근" 단순 추정이었던 것을, 그룹웨어 `commute/worktimes` API가 실제로 계산한 지각 여부(`late`)로 교체. `scripts/sync-attendance.js`가 이제 이 필드를 저장하고, 기존 1~8월 데이터를 재동기화함

### Added
- **근태관리 — 요일 표시** — 출퇴근 표 날짜 옆에 요일 표시(일요일 빨간색, 토요일 파란색), 표 아래에 "빨간색 = 지각" 범례 추가

## [1.15.1] - 2026-08-11

### Fixed
- **근태관리 — 캘린더 이동 달과 동기화** — 근태관리 모달을 열 때 항상 "오늘" 기준 달만 보여주던 문제. 캘린더에서 이전/다음 달로 이동한 뒤 열면 그 달의 연차/출퇴근이 보이도록 `currentYear`/`currentMonth` 기준으로 초기값을 맞춤(연차 현황 플로팅 패널과 동일 로직)

## [1.15.0] - 2026-08-11

### Added
- **근태관리 (최고 관리자 전용)** — 툴바에 admin 전용 "👥 근태관리" 버튼 추가. 연차 현황(부서·직급·총부여/사용/잔여)은 기존 그룹웨어 동기화 데이터를 표로 재사용, 월별 팀 출퇴근 현황은 신규 `/attendance/{yyyy-MM}` Firebase 경로를 표시(그룹웨어 팀 전체 출퇴근 API 연동은 후속 작업). `database.rules.json`에 `attendance` 노드를 admin 두 계정만 읽기/쓰기 가능하도록 추가

## [1.14.11] - 2026-07-14

### Changed
- **2026년 9월 2일 창립기념일 대체휴무 추가** — `HOLIDAYS` 에 `2026-9-2` 사내 휴무일 등록, 빨간날로 표시 (2026년 한정)

## [1.14.10] - 2026-07-14

### Changed
- **일정표 남은 이슈 배지는 숫자만** — 캘린더 일정 태그(`.tag-remaining`)의 배지에서 `남은` 문구 제거, 숫자만 표시(좁은 태그 공간 절약). 툴팁은 `남은 이슈 N건` 유지. 일정 상세보기(모달 목록 `.li-remaining`)는 기존대로 `남은 N` 유지

## [1.14.9] - 2026-07-14

### Fixed
- **일정 상세보기 남은 이슈 배지 컴팩트하게** — 모달 일정 목록의 `남은 N`(`.li-remaining`) 배지가 가로 전체로 늘어나 길던 문제 수정. `.event-list li span { flex:1 }` 공통 규칙에 걸려 배지 span 이 stretch 되던 것을 `flex: 0 0 auto` 로 고정하고 폰트 축소(0.72em→0.6em) → 콘텐츠 폭(~38px)만 차지

## [1.14.8] - 2026-07-14

### Changed
- **완료+남은 배지를 우측 세로 스택으로** — v1.14.7의 태그 전체 줄바꿈(`flex-wrap`, 남은 배지 full-width) 방식을 폐기하고, 완료/최종 + 남은 배지를 우측에 `[완료]` 위 / `[남은]` 아래로 세로 스택(`.tag-badge-stack`, flex-column)으로 표시. 한 칸만 차지해 일정 텍스트가 가로로 밀리지 않으면서 텍스트는 같은 줄에 유지

## [1.14.7] - 2026-07-14

### Changed
- **완료/최종 배지와 겹칠 때 남은 이슈 배지를 아랫줄로** — 일정 태그에 `완료`/`최종` 상태 배지가 있으면 `남은 N` 배지를 인라인이 아니라 아랫줄(`.tag-remaining-row`, `.has-below` 시 `flex-wrap`)에 표시. 두 배지가 가로를 2칸 차지해 일정 텍스트가 밀리던 문제 해결. 상태 배지가 없으면 기존대로 인라인 유지

## [1.14.6] - 2026-07-14

### Changed
- **남은 이슈 개수 동기화** — URL 동기화와 동일하게 같은 `baseEventText`(재확인N차 무시) 일정끼리 `remaining` 값 공유. 한 일정에 값 설정/변경 시 현재 월(in-memory) + 다른 월(Firebase)의 같은 일정에 일괄 전파, 신규 일정은 미입력 시 같은 base 값 자동 상속. 신규 함수 `propagateRemainingForSameText` / `propagateRemainingAcrossOtherMonths` / `findRemainingForSameBase` / `findRemainingForSameBaseInOtherMonths`
- **남은 이슈 배지 로그인 전용 노출** — 캘린더 일정 태그와 모달 일정 목록의 `남은 N` 표시를 `isAuthView`(로그인) 조건으로 게이팅. 비로그인 화면에서는 숨김

## [1.14.5] - 2026-07-14

### Changed
- **남은 이슈 개수를 일정마다 직접 입력** — `남은 N` 배지를 자동 집계(완료/최종 기준, v1.14.4)에서 **사용자 직접 입력값**(`ev.remaining`)으로 변경. 일정 추가 폼과 인라인 편집에 `남은 이슈` 숫자 입력 추가, 0이면 배지 없음
- 일정 목록(모달)에도 현재 남은 개수를 `남은 N` 라벨로 표시. 미입력 시 필드 저장 안 함 → 기존 일정 데이터에 영향 없음

## [1.14.4] - 2026-07-14

### Added
- **일정 태그에 프로젝트별 남은 이슈 배지** — 각 일정 태그에 `[남은 N]` 배지 표시. 같은 제품(프로젝트, `extractProductName` = 일정 첫 줄 기준)에서 아직 `완료`/`최종`이 아닌 일정 개수를 현재 월 전체에서 집계. 완료/최종 처리 시 카운트에서 제외되고 배지도 사라짐
- 기존 상태 모델(`ev.status` = `done`/`final`, `toggleStatus`)을 재사용 — 새 데이터 필드 없이 기존 일정에 영향 없음. 비로그인 사용자에게 숨겨지는 예정(`planned`)·배포일(`color 4`) 일정은 배지 집계에서도 동일한 가시성 필터로 제외해 화면 표시와 개수가 일치

## [1.14.3] - 2026-07-13

### Added
- **방향키(←/→) 월 이동 단축키** — `←` 이전 달, `→` 다음 달. `[◀ 이전]`/`[다음 ▶]` 버튼과 동일하게 `prevMonth()`/`nextMonth()` 호출. 버튼에 단축키 안내 툴팁(`title`) 추가
- 입력 요소(`input`/`textarea`/`select`/`contenteditable`)에 포커스가 있거나 모달·오버레이(`modalOverlay`/`memoOverlay`/`loginOverlay`/`versionOverlay`)가 `active` 일 땐 무시 — 캐럿 이동·타이핑 방해 방지. 조합키(Alt/Ctrl/Meta/Shift) 동반 시에도 무시해 브라우저 기본 단축키 보존

## [1.14.2] - 2026-07-13

### Changed
- **김태언 팀원 퇴사 반영** — `TEAM_MEMBERS` 와 연차 등록 드롭다운(`#memberSelect`)에서 김태언 제외. 신규 연차 등록 대상 및 연차 현황 패널의 기본 행에서 빠짐
- 기존에 등록된 김태언의 연차 기록은 **보존** — 이벤트 데이터에 남아 있는 이름은 `renderLeaveSummary` 에서 계속 집계·표시됨(명단 외 인원으로 분류돼 하단 정렬). 과거 데이터·로그는 삭제하지 않음

## [1.14.1] - 2026-07-13

### Fixed
- **제헌절(7/17) 공휴일 누락** — `HOLIDAYS` 목록에 제헌절이 어느 해에도 없어 빨간날로 표시되지 않던 문제. 2008년 주5일제 도입으로 공휴일에서 빠졌다가 **2026년 재지정(2026-05-11 시행)** 된 것을 반영해 2026~2040 전 연도에 추가
- 제헌절이 토·일과 겹치는 해는 **대체공휴일** 함께 추가 — 2027·2032·2038(토) → 익주 월요일 7/19, 2033·2039(일) → 7/18

## [1.14.0] - 2026-07-13

### Changed
- **지란지교소프트·지란지교데이터 합병 반영** — 2026년 7월 두 회사가 합병되어 같은 회사가 됨에 따라 **2026년 7월 일정표부터** 회사 카테고리 라벨 변경: `지란지교소프트` → **`오피스 사업부문`**, `지란지교데이터` → **`데이터 사업부문`**. 색상 통계 패널 / 색상 픽커 / 일정 편집 색상 버튼에 일관 적용
- 색상 인덱스(저장 값)는 변경 없어 **기존 일정 데이터에 영향 없음** — 라벨만 조회 시점(보고 있는 월) 기준으로 결정(`getColorNames(year, month)`, 2026-07 이상이면 신규 라벨). **2026년 6월 이전 일정표는 기존 `지란지교소프트`/`지란지교데이터` 표기 유지**

## [1.13.3] - 2026-06-05

### Changed
- **김창희 팀원 퇴사 반영** — `TEAM_MEMBERS` 와 연차 등록 드롭다운(`#memberSelect`)에서 김창희 제외. 신규 연차 등록 대상 및 연차 현황 패널의 기본 행에서 빠짐
- 기존에 등록된 김창희의 연차 기록은 **보존** — 이벤트 데이터에 남아 있는 이름은 `renderLeaveSummary` 에서 계속 집계·표시됨(명단 외 인원으로 분류돼 하단 정렬). 과거 데이터·로그는 삭제하지 않음

## [1.13.2] - 2026-06-05

### Fixed
- **연차 패널 연도 이동 중 race condition** — Firebase 콜백 클로저가 `currentYear` 를 참조로 잡아 연도 이동 시 잘못된 연도의 localStorage 키에 저장되던 문제. 콜백에서 연도를 값(`const year`)으로 캡처하도록 수정
- **미동기화 안내 문구** — 폐기된 `npm run sync` 안내를 "확장 팝업에서 로그인 후 동기화" 로 교체
- 확장 팝업 **로그인 실패 메시지가 4초 후 사라지던 문제** — 실패 사유를 다음 시도 전까지 유지하도록 변경

### Changed
- **Chrome 확장(연차 자동 동기화) v1.1.0** — 그룹웨어 SSO에 인증 단계가 추가돼 쿠키(`credentials:include`)만으로 API 인가가 안 되는 문제 대응. `webRequest`로 그룹웨어 페이지가 `jiran.api.groupware.pro` 에 실제로 보내는 인증 헤더(`Authorization` 등)를 가로채 저장하고, 동기화 호출 시 함께 실어 보냄 → 인증 방식이 쿠키든 토큰이든 자동 적응
- 확장 팝업에 **그룹웨어 연동 상태** 표시 — 토큰 확보됨 / 세션 쿠키로 정상(최근 동기화 성공 시) / 미확보(인사 화면 열기 안내). 401/403 알림도 "토큰 미확보"와 "인증 만료(재로그인)"로 구분
- 동기화 실패 에러에 **실제 HTTP 상태코드 + 응답 본문 일부** 포함 — 원인 파악 용이
- 확장 팝업에 **비밀번호 재설정 이메일 전송** 버튼 추가 (이전 'v1.13.3' 커밋 기능 — 앱이 아닌 확장 기능이라 확장 버전 라인으로 편입)

> 참고: 위 변경 중 캘린더 앱(`index.html`) 자체 수정은 연차 패널 race condition·안내 문구 2건이며, 나머지는 동반 업데이트된 Chrome 확장(v1.1.0)의 변경이다.

## [1.13.1] - 2026-06-01

### Fixed
- **일정표 복사 / 사내 게시판용 복사가 반반차 일정이 있는 달에서 동작하지 않던 문제** — `copyAsHTML` / `copyAsHTMLCompat` 의 `leaveColors` 맵에 `am2`(오전반반차) / `pm2`(오후반반차) 색상이 빠져 있어, 해당 연차가 포함된 달(예: 2026년 6월)에서 `undefined.bg` 예외가 발생하며 복사가 중단됨. v1.13.0 에서 연차 유형만 추가하고 복사 함수 갱신이 누락된 회귀. CSS와 동일한 색상(`#5dade2` / `#58d68d`)으로 보강

## [1.13.0] - 2026-05-29

### Added
- **연차 유형에 [오전반반차] / [오후반반차] 추가** — 각각 0.25일로 합계 차감. 등록 버튼·캘린더 태그·사이드 패널·연차 패널 칩 모두 옅은 파랑(`#5dade2`) / 옅은 초록(`#58d68d`) 으로 구분
- 연차 유형 순환(클릭)에 `am2` / `pm2` 추가 — `vacation → am → pm → am2 → pm2 → official` 순

### Changed
- 합계 표시 정밀도: 소수 1자리 → 최대 2자리. 0.25 / 0.75 도 정확히 표시되도록 `parseFloat(toFixed(2))` 적용

## [1.12.1] - 2026-05-29

### Added
- **팀원에 김태언 추가** — `TEAM_MEMBERS` 배열과 연차 등록 드롭다운(`#memberSelect`) 양쪽에 반영. 연차 현황 패널에 행 노출, 등록한 연차도 집계됨

## [1.11.0] - 2026-05-20

### Added
- **회사 카테고리에 [배포일] 추가** (5번째 색상, index=4) — 기존 `[지란지교소프트] [지란지교데이터] [DirectCloud] [기타]` 옆에 노란색(`#eab308`) 박스로 노출. 색상 픽커 / 통계 패널 / 일정표 복사 / 사내 게시판용 복사 / 이미지로 복사 모두 일관 적용
- 노란색 배경에 흰 글자가 안 보이는 문제 — 색상 4 일정은 글자색을 진한 회색(`#1f2937`)으로 자동 전환
- 통계 패널의 회사 분류에 배포일 그룹 추가 (점 `#eab308` / 막대 `#facc15`)
- **배포일(color=4) 일정은 비로그인 사용자에게 숨김** — 🔒 예정 일정과 동일 정책. viewer/admin 로그인 시에만 표시
- **배포일 일정에만 시각적 차별화** — 점선 외곽선(2px dashed `#92400e`) + 🚀 이모지 prefix + drop shadow. planned + 배포일 조합은 `🔒🚀` 둘 다 표시

### Changed
- **v1.10.0 의 [배포일] "상태(status)" 는 회사 카테고리로 재설계되어 제거됨** — `status` 는 다시 [완료]/[최종] 두 가지로 복귀. 정렬 함수의 release 분기도 제거
- **일정 정렬 3단계**: ① `planned` 여부 (일반 위, 예정 아래) → ② 회사 색상 (0~4) → ③ 텍스트 가나다/알파벳

## [1.10.0] - 2026-05-20

### Added
- ~~제품 상태에 [배포일] 추가~~ → **v1.11.0 에서 회사 카테고리로 재설계되어 제거됨**. status 는 [완료]/[최종] 두 가지로 복귀

### Changed
- ~~일정 정렬 4단계로 확장~~ → **v1.11.0 에서 3단계로 단순화** (release 상태 분기 제거)

## [1.9.1] - 2026-05-20

### Changed
- 뷰어 계정에서 상단 툴바의 **[📝 메모] / [일정표 복사] / [사내 게시판용] / [이미지로 복사] / [내보내기]** 버튼 모두 숨김 (`admin-only` 클래스 추가). 뷰어는 캘린더 본문 열람만 가능

## [1.9.0] - 2026-05-20

### Added
- **사용자 역할 분리 — Admin / Viewer**
  - `ADMIN_EMAILS`: `miracle0938@gmail.com`, `miracle38@jiran.com` (편집 가능)
  - `VIEWER_EMAILS`: `wjdrytn1@gmail.com`, `miraclejiran1@gmail.com` (읽기 전용)
- 뷰어 계정은 로그인 후 모든 정보 열람 가능하나 **편집 불가** — 일정 추가/편집/삭제, 휴가 추가, 상태 토글, 드래그, 메모 저장/자동저장, 일정 가져오기, 전체 삭제 등 모든 쓰기 동작에 `requireAdmin()` 가드 적용. 뷰어가 쓰기 시도 시 "읽기 전용 계정입니다" 알림
- 뷰어 계정의 헤더에 **`(읽기 전용)` 표기**, 모달 폼/색상 픽커/휴가 옵션 자동 숨김
- 메모/일정별 메모 에디터는 뷰어에게 `contenteditable="false"` 로 비활성화, 저장 버튼 숨김
- **Firebase 보안 규칙 분리**: `.read` 는 뷰어 포함 모두 허용, `.write` 는 ADMIN_EMAILS 만 허용. ⚠️ Firebase Console 에서 규칙 게시 필요
- **Chrome 확장**: 연차 동기화는 쓰기 작업이라 뷰어 제외. `ADMIN_EMAILS` 만 로그인 가능

### Changed
- `auth-only` 클래스 외에 **`admin-only`** 클래스 도입 — 전체 삭제 / 불러오기 등 위험 쓰기 동작 버튼은 뷰어에게도 숨김
- `localStorage.userRole` 에 역할 캐시 — Firebase 인증 복원 전에도 올바른 권한 UI 표시

## [1.8.2] - 2026-05-20

### Changed
- 허용 사용자 화이트리스트에 `wjdrytn1@gmail.com` 추가 — `index.html` (Firebase Auth 가드) + `extension/common.js` (Chrome 확장) + `database.rules.json` (Firebase 보안 규칙 6경로: calendar/calendar_notes/annual_leave/baseball/tempmail/calendar 2000_00) 모두 반영. **Firebase Console 에서 규칙 게시 필요**

## [1.8.1] - 2026-05-20

### Changed
- 같은 회사(색상) 내 일정 정렬을 **입력 순서가 아니라 텍스트 가나다/알파벳순**으로 통일. 캘린더 타일 / 모달 일정 리스트 / 일정 편집 / 일정표 복사 / 사내 게시판용 복사 **5곳 모두 동일한 비교 함수**(`compareEvForSort`) 사용
  - `ko` 로케일 + `{ sensitivity: 'base', numeric: true }` 옵션 — 한글/영문 혼합과 버전 번호("AIFILTER 2.5" vs "AIFILTER 10.0") 도 자연스럽게 정렬
  - 1차: 회사 색상(color 0→3), 2차: 텍스트 가나다/알파벳

## [1.8.0] - 2026-05-13

### Added
- **[사내 게시판용] 복사 버튼** — 기존 [일정표 복사] 옆에 추가. `style` 속성을 제거하는 sanitizer 환경(사내 그루웨어/게시판)에서도 색상이 유지되도록 **deprecated HTML 속성** (`<td bgcolor>`, `<font color>`) 을 함께 사용
- 일정 박스/휴가 태그를 nested `<table>` 셀로 변환. 클립보드에 `text/html` + `text/plain` 둘 다 포함하여 plain text 만 받는 보드도 폴백 가능
- **[이미지로 복사] 버튼** — `html-to-image`(로컬 `lib/`) 라이브러리로 캘린더를 **PNG 이미지로 클립보드에 복사**. 색상/레이아웃 100% 완벽 (게시판 sanitizer 영향 없음). 단점: 이미지라 텍스트 검색·편집 불가. **외부 CDN 의존성 없음** — 회사 방화벽 영향 없음
- 이미지 복사 시 **상단 툴바 통째로** (이전/다음/오늘/메모/복사/내보내기/불러오기/인쇄/로그인) + **🔒 예정 일정** + **📝 메모 아이콘** + **🔗 URL 아이콘** 등 내부/조작 요소는 캡처에서 자동 제외. 헤더는 한국어 제목 + 영문 부제 둘 다 표시
- 로고 이미지를 **base64 data URL 로 임베드** (`lib/logo-data.js`) — file:// 보안 제약 환경에서도 로고가 함께 캡처됨
- 이미지 캡처 시 **디자인 폭 1600px 으로 강제** + **모바일/태블릿 미디어 쿼리를 인라인 스타일로 우회** — 좁은 뷰포트에서 토요일 컬럼이 잘리거나 제목이 2줄로 줄바꿈되던 문제 해결. 항상 데스크탑 레이아웃으로 캡처
- 사내 게시판용 복사에서 완료/최종 뱃지를 **인라인 텍스트 라벨**로 변경 — 게시판이 `display:inline-table` 을 무시하여 큰 블록 박스로 깨지던 문제 해결

## [1.7.0] - 2026-05-13

### Added
- **월별 제품 통계 패널** (로그인 전용) — 우측에 📈 FAB 버튼 추가. 로그인 사용자에게만 노출 (`.auth-only` 적용). 클릭 시 현재 월에 진행한 일정을 **회사(색상)별로 묶고** 그 안에서 **제품별 건수**를 막대그래프로 시각화
- 제품 그룹 키: 일정 텍스트 **첫 줄 전체**. 단 `재확인N차` (N=1,2,3...) 같은 카운터는 자동으로 `재확인` 으로 통합되어 같은 그룹에 묶임
  - 예: `오피스챗 (기능추가 25) 2차+재확인1차` ↔ `오피스챗 (기능추가 25) 2차+재확인2차` → 같은 그룹 `오피스챗 (기능추가 25) 2차+재확인`
- 제품 이름에 **마우스 오버 시 툴팁**으로 같은 그룹에 속한 일정들의 2번째 줄 이후 내용을 표시 (중복 제거, `&#10;` 로 줄바꿈)
- 패널 하단에 **총 일정 건수 + 제품 종수** 요약. **🔒 예정(planned) 일정은 통계에서 항상 제외** (확정된 작업만 집계). 로그아웃 시 패널이 열려 있으면 자동 닫힘

### Changed
- 캘린더 기본 폰트를 **맑은 고딕(Malgun Gothic) 우선**으로 변경 — Windows 환경에서 일관된 한글 폰트 렌더. Mac에서는 Apple SD Gothic Neo로 폴백
- 캘린더 헤더를 **수직 스택** (제목 위 + 툴바 아래) 으로 재구성 — 11개 버튼이 제목을 밀어 줄바꿈하던 문제 해결. 툴바는 `flex-wrap: wrap` 으로 좁은 화면에서 자연스럽게 다음 행으로 떨어짐

## [1.6.2] - 2026-05-13

### Changed
- URL 자동 전파 시 **"재확인" 마커를 무시** — 다음이 모두 동일한 일정으로 취급되어 URL이 함께 등록/수정/삭제됨:
  - `회의 X` ↔ `회의 X 재확인`
  - `회의 X` ↔ `회의 X (재확인)` / `[재확인]` / `「재확인」`
  - `회의 X` ↔ `회의 X - 재확인` / `: 재확인` / `, 재확인`
  - `회의 X` ↔ `회의 X+재확인1차` / `회의 X+재확인2차` (`+`/`-` 결합 + N차 카운터)
  - `회의 X 2차` ↔ `회의 X 2차+재확인1차` (다른 카운터와 결합된 경우)
- URL 자동 전파가 **다른 월의 일정에도 적용** — 한 달에서 URL 을 등록/수정/삭제하면 모든 월에 걸쳐 같은 base 일정에 일괄 반영됨. Firebase 의 `/calendar` 트리를 한 번 읽고 영향받는 월만 다중 경로 업데이트(`db.ref().update({...})`)로 일괄 쓰기
- 새 일정 추가 시 같은 base 의 기존 일정에 URL 이 있으면 자동 상속 — 현재 월에 없으면 다른 월에서도 비동기로 검색하여 발견 시 새 일정에 URL 을 채워 넣고 재렌더

### Fixed
- 일정이 많은 날짜의 모달을 열 때 **헤더(우측 상단 [전체 삭제]/[×] 버튼)가 화면 위쪽으로 밀려 안 보이던 문제** — 모달 `max-height` 를 92vh 로 제한하고 본문 영역(`.modal-body`)만 내부 스크롤되도록 변경 (flex column 레이아웃)

## [1.6.1] - 2026-05-12

### Changed
- 한 일정에 URL 을 등록/변경하면 **같은 텍스트의 다른 모든 일정**에 같은 URL 이 자동 적용됨. 예: "회의 X" 일정이 5일/12일/19일에 있을 때 한 곳만 등록하면 나머지에도 즉시 반영. URL 을 비우면 같은 텍스트 일정들의 URL 도 같이 제거되어 "동일 텍스트 ⇒ 동일 URL" 불변식 유지
- 캘린더 타일의 🔗 아이콘을 **마우스 오버가 아닌 상시 표시**로 변경. [완료]/[최종] 뱃지 바로 위에 작은 탭 형태로 매달림. **태그 높이는 그대로 유지**되고 텍스트 폭도 줄지 않음

## [1.6.0] - 2026-05-12

### Added
- **일정에 URL 첨부** — 일정 등록/편집 모달에 URL 입력란 추가. `http(s)://` 미입력 시 자동으로 `https://` 부여, `javascript:`/`data:`/`vbscript:`/`file:` 스킴은 보안상 거부
- **새 탭 열기 아이콘** — 캘린더 타일의 일정 위에 마우스 오버 시 우상단에 🔗 아이콘 표시 → 클릭하면 새 탭(`target="_blank" rel="noopener noreferrer"`)으로 이동. **로그인 사용자에게만 노출**
- 모달 일정 리스트에도 동일한 🔗 아이콘 — 일정 텍스트 옆에 한 번 클릭으로 새 탭 이동

## [1.5.0] - 2026-05-12

### Added
- **Chrome 확장 프로그램** (`extension/`) — 그룹웨어 페이지 방문 시 잔여 연차를 자동으로 Firebase 에 동기화. PC가 꺼져 있어도 회사에서 그룹웨어 보다가 자연스럽게 갱신됨
- 확장 팝업에서 Firebase 이메일/비밀번호로 한 번 로그인 → 토큰 자동 갱신 (비밀번호 저장 안 함)
- 트리거: 그룹웨어 페이지 로드 이벤트 + 1시간 알람. 스로틀 1시간으로 API 부하 방지. 팝업의 "지금 동기화" 버튼은 스로틀 무시
- `/annual_leave/_meta.lastSyncSource` 필드 — 어느 경로(`"extension"`/스크래퍼)에서 마지막으로 갱신됐는지 식별

### Changed
- Firebase 규칙 `annual_leave.write` 를 `false` → 본인 이메일 인증 사용자에게 허용으로 변경 — Chrome 확장이 사용자 토큰으로 직접 쓸 수 있도록. 기존 스크래퍼(Admin SDK)는 영향 없음

## [1.4.1] - 2026-05-12

### Fixed
- 월간 연차 현황 패널 진입 시 연간 잔여/사용/총 수치가 **잠시 비었다가 나타나던 깜빡임** 문제 — Firebase 응답을 기다리지 않고 localStorage 캐시에서 즉시 표시 (`events`, 메모와 동일 패턴). `calendar:annualLeave:{year}`, `calendar:annualLeaveMeta` 키로 캐시. 로그아웃 시 캐시도 정리

## [1.4.0] - 2026-05-12

### Added
- 월간 연차 현황 패널에 **연간 잔여 연차** 표시 — 각 팀원 옆에 잔여/사용/총 부여 일수를 한 줄 프로그레스 바로 시각화. 잔여 0일은 빨강, 잔여 20% 이하는 주황으로 강조
- 패널 하단에 마지막 동기화 시각 표시 (`🔄 연차 동기화: 2시간 전 ...`). 7일 이상 경과 시 빨간색 경고
- Node.js 스크래퍼 (`scripts/sync-annual-leave.js`) — Playwright 영구 프로필(SSO 대응)로 `jiran.groupware.pro` 접속 → 첫 실행 시 본인이 직접 SSO 로그인 → 쿠키 저장 → 이후 `annualsummary` API 호출 → Firebase Realtime DB `/annual_leave/{year}/{name}` 에 저장. 비밀번호 저장 없이 동작. 실행: `npm run sync` (첫 로그인/재로그인: `npm run sync:debug`)
- Firebase 룰 `annual_leave` 경로 추가 — 로그인 사용자만 읽기, 쓰기는 Admin SDK(스크래퍼)에서만 가능

## [1.3.2] - 2026-05-06

### Fixed
- 페이지 진입 시 캘린더가 항상 4월로 표시되던 문제 — 초기값이 `2026, 4`로 하드코딩되어 있던 것을 `new Date()` 기반으로 변경. 이제 접속 시 오늘이 포함된 월(2026~2030 범위 내)이 기본 표시됨

## [1.3.1] - 2026-04-28

### Fixed
- 페이지 새로고침 시 캘린더 타일의 메모 📝 아이콘이 늦게 표시되던 문제 — Firebase Auth 비동기 로딩 중에 `currentUser`가 잠깐 null이라 도트 조건이 false였고, 동시에 노트 listener의 가드가 `dayNotes`를 빈 객체로 덮어쓰던 동작 수정. 이제 `loggedIn` 캐시가 있으면 첫 렌더부터 캐시된 메모 데이터로 아이콘이 즉시 표시됨

## [1.3.0] - 2026-04-28

### Added
- **이 날 메모** (날짜별 공유 메모, 로그인 전용) — 사이드 패널에서 선택한 날짜에 메모를 작성/공유. 캘린더 타일에는 메모가 있을 때 📝 아이콘으로 표시
- 메모 입력란에 서식 에디터 적용: 굵게/기울임/밑줄/취소선, 글머리표/번호 매기기, 형광펜·글자색, 링크 삽입, 서식 제거 (Ctrl+B/I/U, Ctrl+Enter로 저장)
- Firebase 룰 `calendar_notes` 경로 추가 — 비로그인 사용자는 메모 데이터에 접근 불가

## [1.2.0] - 2026-04-28

### Added
- 연차 유형에 '공가' 추가 (보라색 배지) — 차감 합계와는 별도로 `공가 N일` 형태로 카운트되어 휴가/반차 차감 일수와 분리 표시됨

## [1.1.2] - 2026-04-24

### Changed
- 버전 정보 팝업의 섹션 라벨을 한국어로 변경: `ADDED` / `CHANGED` / `FIXED` → `추가` / `변경` / `수정`

## [1.1.1] - 2026-04-24

### Added
- 메모 저장 전 모달을 닫아도 편집 내용이 브라우저에 자동 보관되어 다음에 열 때 복원됨 (localStorage 기반 드래프트)

### Changed
- 메모 팝업 타이틀: '📝 팀 공유 메모' → '📝 메모'

### Fixed
- 메모 모달이 빈 영역 클릭으로도 닫혀 드래그로 글 지우다가 꺼지던 문제 — 이제 [×] / [닫기] / Esc 로만 닫힘

## [1.1.0] - 2026-04-24

### Added
- 푸터 버전 번호 클릭 시 앱 내 팝업에서 버전 이력 확인

### Changed
- 푸터 버전 링크: GitHub Releases 외부 이동 → 앱 내 팝업으로 전환 (Releases 링크는 팝업 하단에 보조로 유지)

## [1.0.0] - 2026-04-24

버전 관리를 시작하는 기준점. 이후 모든 변경은 이 문서에 누적 기록한다.

### 주요 기능
- 월간 일정표 (지란지교소프트 품질관리팀)
- Firebase 기반 실시간 동기화 및 로그인
- 일정 CRUD: 타일 클릭 편집, D+클릭 삭제, 드래그 복사(Ctrl/Alt)
- 진행률 게이지 (예정 일정 기간 대비 현재 위치)
- 일정표 HTML 복사 (Shift+클릭 단축키)
- JSON 내보내기 / 불러오기
- 인쇄 지원
- 월간 연차 현황 플로팅 패널
- 메모 모달
- 2026~2030 한국 공휴일 내장

[Unreleased]: https://github.com/miracle38/calendar/compare/v1.0.7...HEAD
[1.0.7]: https://github.com/miracle38/calendar/compare/v1.0.6...v1.0.7
[1.0.6]: https://github.com/miracle38/calendar/compare/v1.0.5...v1.0.6
[1.0.5]: https://github.com/miracle38/calendar/compare/v1.0.4...v1.0.5
[1.0.4]: https://github.com/miracle38/calendar/compare/v1.0.3...v1.0.4
[1.0.3]: https://github.com/miracle38/calendar/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/miracle38/calendar/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/miracle38/calendar/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/miracle38/calendar/releases/tag/v1.0.0
